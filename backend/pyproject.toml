[project]
name = "backend"
version = "0.1.0"
description = "FastAPI backend for Cash Trace — receipt capture bridge for Firefly III"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.1",
    "httpx>=0.28.1",
    "python-multipart>=0.0.22",
    "uvicorn>=0.41.0",
]
